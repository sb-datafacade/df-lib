{
    "Action": {
      "PresentationFormat": "table_value",
      "OutputFormat": "model",
      "ActionType": "Profiling",
      "ActionGroup": "FormatString",
      "Description": "Left fill a numeric string with zeros, up to a given width. If the string is longer than the width, then shorten the string to the width."
    },
    "Tags": ["FormatString"]
}
